import type { D1Database, R2Bucket } from '@cloudflare/workers-types';
import { createRemoteJWKSet, jwtVerify } from 'jose';

type Env = { DB: D1Database; MEDIA: R2Bucket; PUBLIC_ORIGIN: string; ADMIN_SHARED_SECRET?: string; CF_ACCESS_TEAM_DOMAIN?: string; CF_ACCESS_AUDIENCE?: string };
type ContentType = 'page' | 'service' | 'industry' | 'guide' | 'site_settings';
type ContentInput = { contentType: ContentType; slug: string; title: string; summary?: string; body?: unknown; seoTitle?: string; seoDescription?: string; canonicalPath?: string; authorName?: string; reviewerName?: string; status?: 'draft' | 'review' | 'published' | 'archived'; changeNote?: string };
const types = new Set<ContentType>(['page','service','industry','guide','site_settings']);
const json = (data: unknown, init: ResponseInit = {}) => new Response(JSON.stringify(data), { ...init, headers: { 'content-type': 'application/json; charset=utf-8', ...(init.headers || {}) } });
const id = () => crypto.randomUUID();
const validSlug = (slug: string) => /^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(slug);
function assertInput(value: unknown): asserts value is ContentInput { const input=value as Partial<ContentInput>; if (!input || !types.has(input.contentType as ContentType) || typeof input.slug !== 'string' || !validSlug(input.slug) || typeof input.title !== 'string' || input.title.trim().length < 3 || input.title.length > 70) throw new Error('Invalid content payload. A type, URL-safe slug and 3–70 character title are required.'); if ((input.seoTitle?.length ?? 0) > 70 || (input.seoDescription?.length ?? 0) > 170) throw new Error('SEO title or description exceeds CMS content limits.'); }
const jwksByTeam = new Map<string, ReturnType<typeof createRemoteJWKSet>>();
async function actor(request: Request, env: Env) {
  const token = request.headers.get('cf-access-jwt-assertion');
  if (token && env.CF_ACCESS_TEAM_DOMAIN && env.CF_ACCESS_AUDIENCE) {
    const team = env.CF_ACCESS_TEAM_DOMAIN.replace(/^https?:\/\//, '').replace(/\/$/, '');
    const jwks = jwksByTeam.get(team) || createRemoteJWKSet(new URL(`https://${team}/cdn-cgi/access/certs`));
    jwksByTeam.set(team, jwks);
    try { const verified = await jwtVerify(token, jwks, { audience: env.CF_ACCESS_AUDIENCE }); return typeof verified.payload.email === 'string' ? verified.payload.email : 'cloudflare-access-user'; } catch { return null; }
  }
  const secret = request.headers.get('x-vs-admin-secret');
  return env.ADMIN_SHARED_SECRET && secret === env.ADMIN_SHARED_SECRET ? 'service-automation' : null;
}
function cacheHeaders() { return { 'cache-control':'public, max-age=60, s-maxage=300, stale-while-revalidate=86400', 'access-control-allow-origin':'*' }; }
async function saveContent(request: Request, env: Env, existingId?: string) { const user=await actor(request,env); if (!user) return json({error:'Unauthorized admin request.'},{status:401}); const input=await request.json(); assertInput(input); const now=new Date().toISOString(); const existing=existingId ? await env.DB.prepare('SELECT * FROM content_items WHERE id=?').bind(existingId).first<Record<string,unknown>>() : null; if(existingId && !existing) return json({error:'Content item not found.'},{status:404}); const itemId=existingId || id(); const snapshot=JSON.stringify({ ...input, id:itemId, updatedAt:now }); const revision=await env.DB.prepare('SELECT COALESCE(MAX(revision_number),0)+1 AS revision FROM content_revisions WHERE content_id=?').bind(itemId).first<{revision:number}>(); const statements=[env.DB.prepare(`INSERT INTO content_items (id,content_type,slug,status,title,summary,body_json,seo_title,seo_description,canonical_path,author_name,reviewer_name,published_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET content_type=excluded.content_type,slug=excluded.slug,status=excluded.status,title=excluded.title,summary=excluded.summary,body_json=excluded.body_json,seo_title=excluded.seo_title,seo_description=excluded.seo_description,canonical_path=excluded.canonical_path,author_name=excluded.author_name,reviewer_name=excluded.reviewer_name,published_at=CASE WHEN excluded.status='published' THEN COALESCE(content_items.published_at,excluded.published_at) ELSE content_items.published_at END,updated_at=excluded.updated_at`).bind(itemId,input.contentType,input.slug,input.status||'draft',input.title,input.summary||'',JSON.stringify(input.body||{}),input.seoTitle||null,input.seoDescription||null,input.canonicalPath||null,input.authorName||null,input.reviewerName||null,input.status==='published'?now:null,now),env.DB.prepare('INSERT INTO content_revisions (id,content_id,revision_number,snapshot_json,change_note,created_by) VALUES (?,?,?,?,?,?)').bind(id(),itemId,revision?.revision||1,snapshot,input.changeNote||null,user),env.DB.prepare('INSERT INTO audit_events (id,actor,action,entity_type,entity_id,metadata_json) VALUES (?,?,?,?,?,?)').bind(id(),user,existing?'content.updated':'content.created',input.contentType,itemId,JSON.stringify({slug:input.slug,status:input.status||'draft'}))]; await env.DB.batch(statements); if(input.status==='published') await env.DB.prepare("UPDATE content_versions SET version=version+1,updated_at=CURRENT_TIMESTAMP WHERE scope='public-content'").run(); return json({id:itemId,status:input.status||'draft',revision:revision?.revision||1},{status:existing?200:201}); }
export default { async fetch(request: Request, env: Env): Promise<Response> { const url=new URL(request.url); if(request.method==='OPTIONS') return new Response(null,{headers:{'access-control-allow-origin':'*','access-control-allow-methods':'GET,POST,PATCH,OPTIONS','access-control-allow-headers':'content-type,x-vs-admin-secret'}}); if(request.method==='GET' && url.pathname==='/v1/content-version'){const version=await env.DB.prepare("SELECT version,updated_at FROM content_versions WHERE scope='public-content'").first();return json(version||{version:1},{headers:cacheHeaders()});} const publicMatch=url.pathname.match(/^\/v1\/content\/(page|service|industry|guide)\/([a-z0-9-]+)$/); if(request.method==='GET' && publicMatch){const row=await env.DB.prepare("SELECT id,content_type,slug,title,summary,body_json,seo_title,seo_description,canonical_path,author_name,reviewer_name,published_at,updated_at FROM content_items WHERE content_type=? AND slug=? AND status='published'").bind(publicMatch[1],publicMatch[2]).first(); return row?json(row,{headers:cacheHeaders()}):json({error:'Not found.'},{status:404,headers:cacheHeaders()});} if(request.method==='POST' && url.pathname==='/v1/admin/media'){const user=await actor(request,env);if(!user)return json({error:'Unauthorized admin request.'},{status:401});const form=await request.formData();const file=form.get('file');const alt=form.get('altText');if(!(file instanceof File)||file.size>10_000_000||!['image/jpeg','image/png','image/webp','image/avif','application/pdf'].includes(file.type))return json({error:'Upload a JPEG, PNG, WebP, AVIF or PDF under 10 MB.'},{status:400});const mediaId=id();const safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,'-');const objectKey=`uploads/${new Date().toISOString().slice(0,10)}/${mediaId}-${safeName}`;await env.MEDIA.put(objectKey,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}});await env.DB.batch([env.DB.prepare('INSERT INTO media_assets (id,object_key,file_name,mime_type,byte_size,alt_text,created_by) VALUES (?,?,?,?,?,?,?)').bind(mediaId,objectKey,file.name,file.type,file.size,typeof alt==='string'?alt:'',user),env.DB.prepare('INSERT INTO audit_events (id,actor,action,entity_type,entity_id) VALUES (?,?,?,?,?)').bind(id(),user,'media.uploaded','media',mediaId)]);return json({id:mediaId,objectKey,status:'draft'},{status:201});} if(request.method==='GET' && url.pathname==='/v1/admin/content'){ const user=await actor(request,env);if(!user)return json({error:'Unauthorized admin request.'},{status:401});const rows=await env.DB.prepare('SELECT id,content_type,slug,status,title,updated_at,published_at FROM content_items ORDER BY updated_at DESC LIMIT 100').all();return json(rows.results);} if(request.method==='POST' && url.pathname==='/v1/admin/content') return saveContent(request,env); const edit=url.pathname.match(/^\/v1\/admin\/content\/([0-9a-f-]+)$/); if(request.method==='PATCH' && edit) return saveContent(request,env,edit[1]); return json({error:'Route not found.'},{status:404}); } } satisfies ExportedHandler<Env>;
