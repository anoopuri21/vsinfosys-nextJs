import Link from 'next/link'; import { SiteFooter } from '@/components/site-footer'; import { SiteHeader } from '@/components/site-header'; import { industries } from '@/lib/site-data'; export const metadata={title:'Industries'}; export default function Industries(){return <><SiteHeader/><main className="listing"><section className="detailHero"><div className="shell"><p className="eyebrow">Industries</p><h1>Industry context shapes <em>better digital decisions.</em></h1><p>Explore the sectors where clear information, better workflows and conversion-focused experiences matter most.</p></div></section><section className="shell industryGrid">{industries.map(i=><Link href={`/industries/${i.slug}/`} key={i.slug}><small>{i.eyebrow}</small><h2>{i.title}</h2><p>{i.summary}</p><span>Explore industry ↗</span></Link>)}</section></main><SiteFooter/></>}
