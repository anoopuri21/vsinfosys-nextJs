import type { MetadataRoute } from 'next'; export const dynamic = 'force-static'; import { industries, services } from '@/lib/site-data'; export default function sitemap():MetadataRoute.Sitemap{const base='https://vsinfosys.in';return [{url:base,lastModified:new Date(),changeFrequency:'weekly',priority:1},{url:`${base}/services/`,lastModified:new Date(),changeFrequency:'monthly',priority:.9},{url:`${base}/industries/`,lastModified:new Date(),changeFrequency:'monthly',priority:.8},{url:`${base}/technologies/`,lastModified:new Date(),changeFrequency:'monthly',priority:.7},...services.map(s=>({url:`${base}/services/${s.slug}/`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:.8})),...industries.map(i=>({url:`${base}/industries/${i.slug}/`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:.7}))]}
