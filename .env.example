# Public CMS reads only. Never add admin secrets to this file.
NEXT_PUBLIC_CONTENT_API_ORIGIN=https://api.vsinfosys.in

# Set only for the protected admin-site build after Cloudflare Access routing is configured.
NEXT_PUBLIC_CMS_API_ORIGIN=https://admin.vsinfosys.in
