export function initProjectForm(){const form=document.querySelector('[data-project-form]');if(!form)return;const steps=[...form.querySelectorAll('[data-form-step]')];const status=form.querySelector('[data-form-status]');const show=index=>steps.forEach((step,i)=>{step.hidden=i!==index;step.classList.toggle('is-active',i===index)});form.querySelector('.form-next')?.addEventListener('click',()=>{if(!form.querySelector('input[name=need]:checked')){status.textContent='Please choose the closest project type.';return}status.textContent='';show(1);form.querySelector('input[name=name]')?.focus()});form.querySelector('.form-back')?.addEventListener('click',()=>show(0));form.addEventListener('submit',event=>{event.preventDefault();if(!form.checkValidity()){form.reportValidity();return}status.textContent='Prototype form ready. A secure PHP endpoint will be connected for production.'});}
