const labels={responsive:'One structure, intentionally adapted for every screen.',conversion:'A clear message guides attention toward a confident action.',workflow:'Connected logic makes complex movement easier to understand.'};
export function initLab(){const tabs=[...document.querySelectorAll('[data-lab]')];const panels=[...document.querySelectorAll('[data-lab-panel]')];const label=document.querySelector('[data-lab-label]');tabs.forEach(tab=>tab.addEventListener('click',()=>{const key=tab.dataset.lab;tabs.forEach(item=>{const active=item===tab;item.classList.toggle('is-active',active);item.setAttribute('aria-selected',String(active))});panels.forEach(panel=>panel.classList.toggle('is-active',panel.dataset.labPanel===key));if(label)label.textContent=labels[key]}))}
